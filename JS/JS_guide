COMMANDS


arr.push(); добавляє в кінець масиву
arr.unshift(); - добавляє в початок масиву  
arr.pop (); - видаляє останній елемент
arr.shift(); - видаляє перший елемент 
arr.splice(3,2); - видаляє 2 елементи починаючи з 3-го 
arr.splice(2,0, “new”, “new2”); - добавляє елементи перед 2-им елементом 
arr.splice(1,2, “new”, “new2”);  - добавляє 2 елементи і удаляє 1-ий, і 2-ий
arr.reverse() - виводить зворотньо
arr.sort() - сортує строковий тип
arr.join(“.”) - виведе ел.масиву через точку
Concat
arr1; arr2;
total = arr1.concat(arr2); - з’єднує 2 масиви

Slice
arr1;arr2;
var arr3 = arr1.slice(1,3);
1-е - з якого ел.вирізати
2-е - до якого ел. невключно!
Якшо один параметр - то з якого і до останнього








/// when CHECKOUT

4111111111111111
737